<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CheckScam</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .navbar-brand {
            font-weight: bold;
        }

        .card {
            border-radius: 10px;
        }

        .footer {
            background-color: #343a40;
            color: white;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="@Url.Action("Index", "Gr1")">CheckScam</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <form class="d-flex ms-auto me-3" method="get" action="@Url.Action("Index", "Gr1")">
                    <input class="form-control me-2" type="search" name="q" placeholder="Tìm kiếm SĐT hoặc STK" aria-label="Search" />
                    <button class="btn btn-outline-primary" type="submit">Tìm</button>
                </form>
                <ul class="navbar-nav">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="reportDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Tố cáo
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="reportDropdown">
                                <li><a class="dropdown-item" href="@Url.Action("Report", "Gr1")">Tố cáo SĐT</a></li>
                                <li><a class="dropdown-item" href="@Url.Action("ReportUrl", "Gr1")">Tố cáo URL</a></li>
                            </ul>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Logout", "Gr1")">Đăng xuất (@User.Identity.Name)</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Login", "Gr1")">Đăng nhập</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Register", "Gr1")">Đăng ký</a>
                        </li>
                    }
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Menu
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "Gr1")">Trang chủ</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Instructions", "Gr1")">Hướng dẫn sử dụng</a></li>
                            @if (User.IsInRole("Superuser"))
                            {
                                <li><a class="dropdown-item" href="@Url.Action("Index", "AdminDashboard")">Admin</a></li>
                            }
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        @RenderBody()
    </div>

    <footer class="footer mt-5 py-4">
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5>Về chúng tôi</h5>
                    <p>CheckScam giúp kiểm tra và cảnh báo lừa đảo qua số điện thoại, tài khoản ngân hàng, URL, ...</p>
                </div>
                <div class="col-md-3">
                    <h5>Tính năng</h5>
                    <ul class="list-unstyled">
                        <li>Kiểm tra URL</li>
                        <li>Kiểm tra số điện thoại</li>
                        <li>Tố cáo lừa đảo</li>
                        <li>Lịch sử tra cứu</li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Thông tin</h5>
                    <ul class="list-unstyled">
                        <li>Chính sách bảo mật</li>
                        <li>Điều khoản sử dụng</li>
                        <li>FAQ</li>
                        <li>Hướng dẫn</li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Liên hệ</h5>
                    <ul class="list-unstyled">
                        <li>support@checkscam.vn</li>
                        <li>ĐH HUTECH</li>
                        <li>0123 456 789</li>
                    </ul>
                </div>
            </div>
            <hr />
            <p class="text-center">© 2025 - Website lọc thông tin lừa đảo | ĐH HUTECH</p>
        </div>
    </footer>
</body>
</html>