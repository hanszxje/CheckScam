<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CheckScam</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="bg-dark text-white p-3">
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-dark">
                <a class="navbar-brand" href="@Url.Action("Index", "Gr1")">CheckScam</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <form class="d-flex ms-auto me-3" method="get" asp-action="Index" asp-controller="Gr1">
                        <input class="form-control me-2" type="search" name="q" placeholder="Tìm kiếm" value="@Context.Request.Query["q"]">
                        <button class="btn btn-outline-light" type="submit">Tìm</button>
                    </form>
                    <ul class="navbar-nav">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Report", "Gr1")">Tố cáo</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Logout", "Gr1")">Đăng xuất (@User.Identity.Name)</a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Login", "Gr1")">Đăng nhập</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Register", "Gr1")">Đăng ký</a>
                            </li>
                        }
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                Menu
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="@Url.Action("Index", "Gr1")">Trang chủ</a></li>
                                <li><a class="dropdown-item" href="@Url.Action("Instructions", "Gr1")">Hướng dẫn sử dụng</a></li>
                                @if (User.IsInRole("Superuser"))
                                {
                                    <li><a class="dropdown-item" href="@Url.Action("Index", "AdminDashboard")">Admin</a></li>
                                }
                            </ul>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </header>

    <main class="container mt-4">
        @RenderBody()
    </main>

    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5>Về chúng tôi</h5>
                    <p>CheckScam giúp kiểm tra và cảnh báo lừa đảo qua số điện thoại, tài khoản ngân hàng, URL, ...</p>
                </div>
                <div class="col-md-3">
                    <h5>Tính năng</h5>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white">Kiểm tra URL</a></li>
                        <li><a href="#" class="text-white">Kiểm tra số điện thoại</a></li>
                        <li><a href="@Url.Action("Report", "Gr1")" class="text-white">Tố cáo lừa đảo</a></li>
                        <li><a href="#" class="text-white">Lịch sử tra cứu</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Thông tin</h5>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white">Chính sách bảo mật</a></li>
                        <li><a href="#" class="text-white">Điều khoản sử dụng</a></li>
                        <li><a href="#" class="text-white">FAQ</a></li>
                        <li><a href="@Url.Action("Instructions", "Gr1")" class="text-white">Hướng dẫn</a></li>
                    </ul>
                </div>  
                <div class="col-md-3">
                    <h5>Liên hệ</h5>
                    <ul class="list-unstyled">
                        <li><a href="mailto:support@checkscam.vn" class="text-white">support@checkscam.vn</a></li>
                        <li><span class="text-white">ĐH HUTECH</span></li>
                        <li><span class="text-white">0123 456 789</span></li>
                    </ul>
                </div>
            </div>
            <hr class="bg-light">
            <p class="text-center">© 2025 - Website lọc thông tin lừa đảo | ĐH HUTECH</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>